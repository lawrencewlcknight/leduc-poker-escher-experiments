[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leduc-poker-escher-experiments"
version = "0.1.0"
description = "Leduc poker ESCHER experiments for neural CFR thesis work"
requires-python = ">=3.9,<3.10"

[tool.setuptools.packages.find]
where = ["."]
include = [
  "escher_poker",
  "escher_poker.*",
  "experiments",
  "experiments.*",
]
exclude = [
  "outputs*",
  "notebooks*",
  "thesis_artifacts*",
  "gcp*",
  "tests*",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "smoke: end-to-end smoke tests",
]

# ESCHER Bottleneck Architecture Sweep

Single-seed diagnostic sweep comparing bottleneck, non-bottleneck, wide, and expanding MLP shapes.

```bash
python -m experiments.leduc_poker.escher_bottleneck_architecture_sweep.run
```


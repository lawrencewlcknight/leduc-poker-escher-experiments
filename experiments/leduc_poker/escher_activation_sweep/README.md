# ESCHER Activation Sweep

Single-seed diagnostic sweep testing common activation functions under the Experiment 13 budget.

```bash
python -m experiments.leduc_poker.escher_activation_sweep.run
```


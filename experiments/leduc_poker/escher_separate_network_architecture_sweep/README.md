# ESCHER Separate-Network Architecture Sweep

Single-seed diagnostic sweep that starts from the Experiment 13 author-budget configuration and changes only the relative capacity allocated to the policy, regret, and value networks.

Run locally with:

```bash
python -m experiments.leduc_poker.escher_separate_network_architecture_sweep.run
```


# ESCHER Layer-Normalisation Ablation

Single-seed diagnostic sweep testing whether the currently always-on layer normalisation helps or hurts each ESCHER network.

```bash
python -m experiments.leduc_poker.escher_layer_norm_ablation.run
```


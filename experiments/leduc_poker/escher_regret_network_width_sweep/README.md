# ESCHER Regret-Network Width Sweep

Single-seed diagnostic sweep that isolates regret-network capacity while holding policy and value networks at the Experiment 13 reference size.

```bash
python -m experiments.leduc_poker.escher_regret_network_width_sweep.run
```

